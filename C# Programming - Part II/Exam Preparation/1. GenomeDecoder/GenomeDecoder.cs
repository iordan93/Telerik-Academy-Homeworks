using System;
using System.Text;

    class GenomeDecoder
    {
        static void Main()
        {
            string NandM = Console.ReadLine();
            string[] NM = NandM.Split(' ');
            ushort N = ushort.Parse(NM[0]);
            ushort M = ushort.Parse(NM[0]);

            string genome = Console.ReadLine();
            StringBuilder sb = new StringBuilder(genome);
            
        }
    }